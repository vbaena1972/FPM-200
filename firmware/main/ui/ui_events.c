/* Handlers de eventos de configuración de la nueva HMI.
 *
 * La lógica de negocio (mapeo AppConfig <-> widgets, validaciones, llamadas a
 * wifi_mgr/eth_mgr/transport_ble/cert_store) se porta desde el HMI anterior
 * (respaldado en el scratchpad) a medida que se implementan las pantallas de
 * configuración en las Fases 4-5.
 *
 * Por ahora este archivo queda intencionalmente vacío para mantener el build
 * limpio tras la eliminación del HMI SquareLine. */

#include "ui_events.h"
