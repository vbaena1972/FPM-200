# Name,   Type, SubType,  Offset,   Size,     Flags
# 1.5.22: NVS 24 KB -> 84 KB (was near full: certs + config + PHY cal + consumption
# checkpoints; a full NVS makes main.c erase it). Uses the free gap before app0,
# so app0/app1 keep their offsets. otadata moves -> requires a full erase-flash
# once (see docs/HANDOFF.md). coredump added after app1 (crash analysis in field).
# storage (FAT) is not mounted by the firmware; kept at 6 MB after coredump.
nvs,      data, nvs,      0x9000,   0x15000,
otadata,  data, ota,      0x1E000,  0x2000,
app0,     app,  ota_0,    0x20000,  0x300000,
app1,     app,  ota_1,    0x320000, 0x300000,
coredump, data, coredump, 0x620000, 0x20000,
storage,  data, fat,      0x640000, 0x600000,
